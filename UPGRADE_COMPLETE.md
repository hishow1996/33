# Upgrade baseline

33 Terminal 1.3.0 baseline is prepared with additive terminal infrastructure. Existing Ubuntu rootfs and Native PTY implementation are intentionally preserved until a real Android build/device validation can be performed.
