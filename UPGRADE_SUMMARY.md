# 1.3.0 upgrade summary

This release adds reusable terminal infrastructure while deliberately preserving the existing Ubuntu bootstrap and Native PTY path. Advanced cell/grid rendering, multi-session, SAF and bundled PRoot are isolated as the next validation stage.
